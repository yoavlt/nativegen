defmodule Mix.Swiftgen do

  @moduledoc false

  @doc """
  Copied files from source dir to target dir
  """
  def copy_from(apps, source_dir, target_dir, binding, mapping) when is_list(mapping) do
  end
end
